# CLAUDE.md

```
@AGENTS.md
```

This file is a 1-line shim. Claude Code reads `CLAUDE.md` by convention; the `@AGENTS.md` directive imports the canonical agent rules from `AGENTS.md` at the same level.

Same source of truth, different harness. Move to a new tool tomorrow (Codex, Cursor, whatever ships next), add a 1-line shim, and the persona moves with it.
